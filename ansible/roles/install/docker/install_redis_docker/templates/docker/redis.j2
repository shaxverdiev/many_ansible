version: '3.9'

services:
  redis:
    image: "{{ image }}"
    container_name: "{{ cont_name }}"
    restart: always
    volumes:
      - "{{ volume_1 }}/redis.conf:/usr/local/etc/redis/redis.conf:ro"
      - "{{ volume_2 }}:/data"
    ports:
      - "{{ external_port }}:6379"
    command: ["redis-server", "/usr/local/etc/redis/redis.conf"]
