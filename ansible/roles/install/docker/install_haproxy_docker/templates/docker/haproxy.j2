version: '3.9'

services:
  haproxy:
    image: "{{ image }}"
    container_name: "{{ cont_name}}"
    ports:
      - "{{ external_port }}:80" 
    volumes:
      - "{{ volume_1 }}:/usr/local/etc/haproxy/haproxy.cfg:ro"
    restart: unless-stopped