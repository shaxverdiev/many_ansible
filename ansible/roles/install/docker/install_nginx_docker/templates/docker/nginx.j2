version: '3'

services:
  nginx:
    image: "{{ image }}"
    container_name: "{{ cont_name }}"
    restart: always
    volumes:
      - "{{ volume_1 }}/nginx.conf:/etc/nginx/nginx.conf:ro"
    ports:
      - "{{ external_port }}:80"

