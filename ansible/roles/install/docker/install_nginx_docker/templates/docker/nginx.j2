version: '3.9'

services:
  nginx:
    image: "{{ image }}"
    container_name: "{{ cont_name }}" 
    ports:
      - "{{ external_port }}:80"
    volumes:
      - "{{ volume_1 }}/nginx.conf:/etc/nginx/nginx.conf:ro"
    restart: unless-stopped
