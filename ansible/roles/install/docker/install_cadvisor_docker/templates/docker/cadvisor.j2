version: '3'

services:
  cadvisor:
    image: {{ image }}
    container_name: {{ cont_name }}
    restart: always
    volumes:
      - /:/rootfs:ro
      - /var/run:/var/run:rw
      - /sys:/sys:ro
      - /var/lib/docker/:/var/lib/docker:ro
    ports:
      - {{ external_port }}:8080

